# PDF 生成说明

## 已生成的文件

- **HTML 文件**: `2025.1.1形变处理工作汇报.html`
  - 包含完整的 MathJax 配置
  - 所有 LaTeX 公式都会通过 MathJax 正确渲染
  - 已优化打印样式

## 生成 PDF 的方法（推荐）

### 方法 1: 使用浏览器打印（最推荐，确保公式正确渲染）

1. **打开 HTML 文件**
   ```bash
   # 在文件管理器中双击打开，或使用浏览器打开：
   file:///home/sjw/data0/lsx/S3PO_baseline/docs/2025.1.1形变处理工作汇报.html
   ```

2. **等待公式渲染**
   - 打开文件后，等待 2-3 秒让 MathJax 完成公式渲染
   - 页面右上角会显示 "✓ 公式渲染完成！" 的提示（如果看到）

3. **打印为 PDF**
   - 按 `Ctrl+P` (Linux/Windows) 或 `Cmd+P` (Mac) 打开打印对话框
   - 选择 "另存为 PDF" 或 "打印到 PDF"
   - 设置选项：
     - 纸张大小: A4
     - 边距: 默认或自定义
     - 背景图形: 启用（确保表格背景色正确显示）
   - 点击 "保存" 或 "打印"

4. **保存文件**
   - 保存为: `2025.1.1形变处理工作汇报.pdf`

### 方法 2: 使用命令行工具（如果已安装）

#### 使用 wkhtmltopdf
```bash
wkhtmltopdf docs/2025.1.1形变处理工作汇报.html docs/2025.1.1形变处理工作汇报.pdf
```

#### 使用 pandoc
```bash
pandoc docs/2025.1.1形变处理工作汇报.md -o docs/2025.1.1形变处理工作汇报.pdf \
  --pdf-engine=xelatex \
  -V CJKmainfont='Microsoft YaHei' \
  -V geometry:margin=2cm
```

## 公式渲染检查

HTML 文件已配置 MathJax，支持以下 LaTeX 语法：

- **内联公式**: `$公式$` 或 `\(公式\)`
- **显示公式**: `$$公式$$` 或 `\[公式\]`

所有公式都会在浏览器中正确渲染。

## 注意事项

1. **公式渲染**: 只有使用浏览器打开 HTML 文件才能确保公式正确渲染
2. **打印设置**: 建议启用 "背景图形" 选项，以确保表格和代码块的背景色正确显示
3. **页面布局**: HTML 文件已优化打印样式，包括页面分页、边距等

## 文件位置

- Markdown 源文件: `docs/2025.1.1形变处理工作汇报.md`
- HTML 文件: `docs/2025.1.1形变处理工作汇报.html`
- PDF 文件（待生成）: `docs/2025.1.1形变处理工作汇报.pdf`


