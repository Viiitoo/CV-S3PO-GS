# 曲率方法（Curvature Method）说明

## 什么是曲率方法？

**曲率方法**是一种基于点云几何特性（曲率）的边缘提取算法，它是STAR-Edge/LocalSH的**备选方案**。

## 核心原理

### 1. 基本思想

在点云中，**边缘点**通常位于曲率较大的地方（表面变化剧烈），而**平面点**的曲率较小（表面平滑）。

- **曲率高** → 表面快速变化 → 可能是边缘
- **曲率低** → 表面平滑 → 不是边缘

### 2. 算法步骤

#### 步骤1：计算法线（Normal Computation）

对每个点，计算其表面法线向量：

```python
# 使用KNN（K近邻）找到每个点周围的邻居点
neighbors = find_knn(points, point_i, k=20)

# 使用PCA（主成分分析）计算法线
# 法线方向 = 最小特征值对应的特征向量
normal = compute_normal_from_neighbors(neighbors)
```

**原理**：法线指向表面垂直方向，法线的变化反映表面的弯曲程度。

#### 步骤2：计算曲率（Curvature Computation）

对每个点，计算其曲率值：

```python
# 计算该点法线与邻居点法线的差异
for each neighbor_j:
    diff = 1 - abs(dot(normal_i, normal_j))  # 法线点积，差异越大曲率越大

curvature = mean(diffs)  # 平均差异 = 曲率
```

**原理**：
- 如果点位于平面上，邻居法线方向相似 → 差异小 → 曲率低
- 如果点位于边缘，邻居法线方向差异大 → 差异大 → 曲率高

#### 步骤3：提取边缘（Edge Extraction）

根据曲率值筛选边缘点：

```python
# 方法1：阈值法
edge_points = points[curvature > threshold]  # 曲率超过阈值的点

# 方法2：比例法
top_k_points = sort_by_curvature(points)[-20%]  # 取曲率最高的20%点

# 组合两种方法
edge_points = threshold_method | ratio_method
```

## 代码实现

### 法线计算

```python
def compute_normals(self, points, k=20):
    """
    计算点云法线
    
    方法：PCA（主成分分析）
    - 对每个点的KNN邻域进行PCA
    - 法线 = 最小特征值对应的特征向量
    """
    # 1. 找到K个最近邻
    neighbors = find_knn(points, k)
    
    # 2. 计算邻域协方差矩阵
    centered = neighbors - neighbors.mean(axis=0)
    cov = centered.T @ centered
    
    # 3. SVD分解，获取最小特征值对应的特征向量（法线）
    _, _, vh = np.linalg.svd(cov)
    normal = vh[-1]  # 最小特征值对应的特征向量
    
    return normals
```

### 曲率计算

```python
def compute_curvature(self, points, normals, k=20):
    """
    计算点云曲率
    
    方法：法线变化量
    - 计算点法线与邻居法线的差异
    - 差异越大，曲率越大
    """
    for each point_i:
        neighbors = find_knn(points, point_i, k)
        
        # 计算法线差异
        normal_diffs = []
        for neighbor_j in neighbors:
            diff = 1.0 - abs(dot(normal_i, normal_j))
            normal_diffs.append(diff)
        
        curvature[i] = mean(normal_diffs)  # 平均差异 = 曲率
    
    return curvatures
```

### 边缘提取

```python
def extract_edges_curvature(self, points):
    """
    使用曲率方法提取边缘点
    """
    # 1. 计算法线
    normals = self.compute_normals(points)
    
    # 2. 计算曲率
    curvatures = self.compute_curvature(points, normals)
    
    # 3. 提取边缘
    # 方法1：阈值过滤
    threshold_mask = curvatures > 0.01
    
    # 方法2：取前20%高曲率点
    num_edges = int(len(points) * 0.2)
    top_indices = np.argsort(curvatures)[-num_edges:]
    
    # 组合
    edge_mask = threshold_mask | (indices in top_indices)
    
    return edge_mask, curvatures
```

## 曲率方法 vs LocalSH方法

### 曲率方法（Curvature Method）

| 特性 | 说明 |
|------|------|
| **原理** | 基于几何特性（法线、曲率） |
| **优点** | ✅ 实现简单<br>✅ 无需外部依赖<br>✅ 计算速度快<br>✅ 内存占用小 |
| **缺点** | ❌ 对噪声敏感<br>❌ 精度较低<br>❌ 难以处理复杂结构 |
| **适用场景** | 简单场景、快速原型、备选方案 |

### LocalSH方法（STAR-Edge核心算法）

| 特性 | 说明 |
|------|------|
| **原理** | 基于球谐函数（Spherical Harmonics）的局部描述符 |
| **优点** | ✅ 精度高<br>✅ 鲁棒性强<br>✅ 适合薄壁结构<br>✅ 对噪声不敏感 |
| **缺点** | ❌ 需要编译C++扩展<br>❌ 计算稍慢<br>❌ 依赖fftw库 |
| **适用场景** | 复杂场景、高精度要求、薄壁结构 |

## 为什么需要曲率方法作为备选？

1. **兼容性**：LocalSH需要编译C++扩展，可能因环境问题无法使用
2. **鲁棒性**：确保在LocalSH不可用时系统仍能正常工作
3. **灵活性**：可以在不同场景下选择合适的算法

## 使用示例

### 自动选择方法

```python
from utils.edge_extraction import EdgeExtractor

extractor = EdgeExtractor()

# 自动选择：优先LocalSH，失败时使用曲率方法
edge_mask, edge_scores = extractor.extract_edges(points)
```

### 强制使用曲率方法

```python
# 禁用LocalSH
extractor.use_localsh = False

# 直接使用曲率方法
edge_mask, edge_scores = extractor.extract_edges_curvature(points)
```

## 参数调优

### 曲率阈值（curvature_threshold）

```yaml
edge_extraction:
  curvature_threshold: 0.01  # 默认值
```

- **较大值**（0.05）：只检测明显边缘，漏检多
- **较小值**（0.005）：检测更多边缘，误检多

### KNN邻居数（knn_neighbors）

```yaml
edge_extraction:
  knn_neighbors: 20  # 默认值
```

- **较大值**（30-50）：更平滑的法线，但可能丢失细节
- **较小值**（10-15）：更敏感，但可能受噪声影响

### 边缘比例（edge_ratio）

```yaml
edge_extraction:
  edge_ratio: 0.2  # 保留20%最显著的边缘点
```

- **较大值**（0.3）：保留更多边缘点
- **较小值**（0.1）：只保留最明显的边缘

## 实际效果对比

### 曲率方法
- ✅ **快速**：处理100K点云约需数秒
- ✅ **轻量**：无需外部依赖
- ⚠️ **精度中等**：适合简单场景

### LocalSH方法
- ✅ **高精度**：特别适合薄壁结构
- ✅ **鲁棒**：对噪声和复杂几何适应性强
- ⚠️ **稍慢**：需要更多计算资源

## 总结

**曲率方法**是一种简单有效的边缘提取算法，基于点云几何特性（法线和曲率）。它作为STAR-Edge/LocalSH的备选方案，确保在LocalSH不可用时系统仍能正常工作。

**核心优势**：
- ✅ 无需编译，开箱即用
- ✅ 计算快速
- ✅ 实现简单，易于理解

**推荐使用场景**：
- LocalSH不可用时（自动回退）
- 简单场景的快速测试
- 不需要最高精度时



